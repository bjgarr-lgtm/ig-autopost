{
  "name": "ig-autopost",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build:web": "bun --cwd ../../apps/web run build",
    "deploy": "bun run build:web && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260106.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.21.2"
  }
}